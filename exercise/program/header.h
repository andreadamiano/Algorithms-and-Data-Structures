#ifndef HEADER_H // include guard (to prevent multiple inclusions of header files)
#define HEADER_H //macro definition (if it's already defined, don't define it again)

// Code to include if the macro MACRO_NAME is defined
void SayHello();
void SayGoodbye();

#endif // Ends the conditional inclusion block